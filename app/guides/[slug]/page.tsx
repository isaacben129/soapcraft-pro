import Link from "next/link";
export default async function GuidePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main><Link href="/tools">Back to tools</Link><h1>{slug.replaceAll("-", " ")}</h1><p>This guide explains the inputs, assumptions, and boundaries of the SoapCraft Pro workflow. Calculations remain separate from commercial recommendations and safety claims.</p></main>}
