export interface SellerPackInput { businessName: string; products: Array<{ name: string; price: number; ingredients?: string }>; contact?: string; }
export function generateSellerPack(input: SellerPackInput): string { if (!input.businessName.trim() || input.products.length === 0) throw new Error("Business name and at least one product are required"); const lines = [`# ${input.businessName}`, "", "## Product price list", "", ...input.products.map(p => `### ${p.name}\nPrice: ${p.price.toFixed(2)}${p.ingredients ? `\nIngredients: ${p.ingredients}` : ""}`), "", input.contact ? `Contact: ${input.contact}` : "", "", "Generated by SoapCraft Pro Seller Pack"]; return lines.join("\n"); }
